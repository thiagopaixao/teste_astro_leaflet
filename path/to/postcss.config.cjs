module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
        'postcss-nested': {}, // Added nested plugin as per the previous file
        // Add other plugins as necessary
    }
};
