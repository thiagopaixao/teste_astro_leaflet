---
const { backgroundImage, title } = Astro.props;
---

<header class="bg-cover bg-center h-screen flex items-center justify-center" style={`background-image: url('${backgroundImage}')`}>
  <h1 class="text-4xl font-bold text-white">{title}</h1>
</header>
